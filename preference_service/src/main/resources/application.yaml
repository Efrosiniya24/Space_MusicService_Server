spring:
  application:
    name: PREFERENCE_SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/preference
    password: 1111
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    open-in-view: false

server:
  port: 8084

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    hostname: preferences
    prefer-ip-address: false